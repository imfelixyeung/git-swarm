---
"@imfelixyeung/git-swarm": patch
---

Validate configured repository paths and exit non-zero with a hint to run `git-swarm config refresh` when a path is missing, not a directory, or not a git repository